package operations is

    type BinaryOp is (

        GREATER_THAN,
        GREATER_EQUAL,
        LESS_THAN,
        LESS_EQUAL,
        EQUAL,
        DIFFERENT
    );

    type UnaryOp is (

        INVERSE,
        MINUS
    );


end operations;